This is a movie recommendation agent.

DO NOT change the model from gemma4:31b-cloud

After every change, run the test suite to verify correctness:

    OLLAMA_API_KEY=your_key_here python test.py

All tests must pass before the implementation is considered complete.

